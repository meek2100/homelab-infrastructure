services:
  nginx-proxy-manager:
    image: jc21/nginx-proxy-manager:latest
    container_name: nginx-proxy-manager
    network_mode: host
    environment:
#      - PUID=${PUID}
#      - PGID=${GUID}
      - TZ=${TZ}
    volumes:
      - ${BASE_PATH}/nginx-proxy-manager/config:/data
      - ${BASE_PATH}/nginx-proxy-manager/certs:/etc/letsencrypt
#    ports:
#      - "80:80"
#      - "81:81"
#      - "443:443"
    restart: unless-stopped
#    networks:
#      vlan1_network:
#        mac_address: 02:F5:58:97:8E:44
#        ipv4_address: 192.168.1.50 # Assign a static IP for AdGuardHome


#networks:
#  vlan1_network: # This network is for untagged traffic that should be on vlan1, it is created by my custom systemd script.
#    external: true